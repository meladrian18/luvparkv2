import 'package:flutter/widgets.dart';

class InternetConn extends StatelessWidget {
  const InternetConn({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("No Internet"),
    );
  }
}
